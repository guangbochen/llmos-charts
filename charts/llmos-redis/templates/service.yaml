apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmos-redis.fullname" . }}
  labels:
    {{- include "llmos-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llmos-redis.selectorLabels" . | nindent 4 }}
