apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llmos-redis.fullname" . }}-test-connection"
  labels:
    {{- include "llmos-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llmos-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
